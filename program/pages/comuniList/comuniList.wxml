<!--pages/evaList/evaList.wxml-->
<import src="../template/template.wxml"/>

<template is="title" data="{{title: '聊天列表'}}"></template>

<view class="comItem border" wx:for="{{comList}}" id="{{index}}" bindtap="toCom" id="{{item.name}}">
  {{item.name}}
</view>